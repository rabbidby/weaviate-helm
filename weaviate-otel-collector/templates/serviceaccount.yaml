{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "woc.serviceAccountName" . }}
  labels:
    {{- include "woc.labels" . | nindent 4 }}
{{- end }}
